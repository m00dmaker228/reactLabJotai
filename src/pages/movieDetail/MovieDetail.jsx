

import React from 'react'
import { MovieDetailComponent } from '../../components/movieDetail/MovieDetailComponent'

export const MovieDetail = ({movies}) => {
  return (
    <MovieDetailComponent movies={movies}/>
  )
}
